//List como lo sugiere la plataforma

/* import React from "react";

const List = () => {
    return(
        <div>
            {products.map((product, index) => {
                return <p key={index}> {product.title}, {product.price}, {product.description}</p>
            })}
        </div>
    )
}

export default List; */

//-------------------------------------------------------------